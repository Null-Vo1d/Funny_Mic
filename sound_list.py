import sounddevice as sd

print(sd.query_devices())



#Run this to get a list of sound devices then change 'device=(in, out)' with your numbers
#the two selections with arrows on them will be the windows selected in and out.